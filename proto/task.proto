syntax = "proto3";

package proto;

service Task {
    rpc Create(Request) returns (Response) {}
}

message Request {
    int64 execTime = 1;
}

message Response {
    string status = 1;
    int64 code = 2;
    int64 id = 3;
}
